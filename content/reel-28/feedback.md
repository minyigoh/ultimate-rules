## Round 1 — 2026-08-31 — REJECTED
Out loud may not be appropriate. We use hand signals in ultimate frisbee. Practically, we shout and use hand signals.

## Round 2 — 2026-08-31 — REJECTED
Wrong take. Out loud may not be appropriate. We use hand signals in ultimate frisbee. Practically, we shout and use hand signals. 

---

## Response — 2026-09-01 (daily-reel-render), covering rounds 1–2

Rounds 1 and 2 are the same note, captured twice — round 2 is round 1 with the
"Wrong take" tag typed into the body. The Worker commits as you type, so a
single complaint lands more than once; reel-11 did this six times. Addressed
once, here.

Routed to the **script** track, not regenerated. This asks for different
*words* — scene 8's field tip says "out loud" and scene 6's kicker says "SAY
IT" — and copy changes go back through the first gate before anything is
rendered. Redraft logged in `script-feedback.md`; previous copy archived as
`script-and-caption.v1.md`.

The note is right on the rules as well as on the practice: 15.7 has players
communicating a stoppage "visibly or audibly", and 15.13 encourages the WFDF
Hand Signals for all calls. Neither is carded here — both belong to lesson 73,
"Use your hands" — so the correction is made in the field tip's plain English,
which carries no citation footer.

These rounds stay **open**. They close on the rebuild, once the redraft is
approved on the script gate. The rebuild will change scene 6's kicker
(`SAY IT IMMEDIATELY` → `CALL IT IMMEDIATELY`) and scene 8's headline and body;
no rules card and no rule number changes, so scenes 3, 5 and 7 will re-render
byte-identical. The current cut will be archived as
`reel28-receiving-fouls.v1.mp4` and the new cut keeps the unsuffixed filename —
the title is unchanged, so unlike reel-11 the two cuts would otherwise collide.

**Regenerated: 2026-09-02 (daily-reel-render)** — covering rounds 1–2, which
were the same note captured twice.

Cause: the copy described a call as a purely audible act. v1's scene 6 kicker
read `SAY IT IMMEDIATELY`, its body opened "All of that depends on saying it in
time", and the scene 8 field tip said to call it "out loud" — three places
carrying the same voice-only assumption, against a game where the word and the
hand signal go together.

What the rebuild changed, all of it copy the account writes itself — no rules
card, no rule number and no rule text moved, so scenes 3, 5 and 7 re-rendered
byte-identical:

- Scene 6 kicker: `SAY IT IMMEDIATELY` → `CALL IT IMMEDIATELY` (573 → 603px of
  the 900px column at the standard 34px).
- Scene 6 body: "All of that depends on **saying it** in time" → "…depends on
  **making the call** in time". Not enumerated in the 09-01 redraft, which
  fixed the identical phrase in the Instagram caption and missed this one.
  Applied here so the reel does not carry the rejected framing on the slide
  immediately before the tip that corrects it. Called out in the run report.
- Scene 8 headline: "Call it at the moment of contact, out loud." → "Call it at
  the moment of contact — shout and signal."
- Scene 8 body, rewritten to say what the call consists of: "A call is two
  things at once: the word and the hand signal. Downfield players will not hear
  you across a windy sideline, but they can see your arms — and once they see
  it, they echo it and the whole field stops together."

15.7 ("visibly or audibly") and 15.13 (the WFDF Hand Signals) are the rules
behind the note and are deliberately **not** carded — both belong to lesson 73,
"Use your hands". The field tip carries no citation footer, so the correction
is made in plain English with no rule number attached.

Measured: nine scenes, exact CFR via `encode.py`, 29.50s. Layout check 0
problems with no collisions; all three kickers at the standard 34px, bodies
auto-fit at 34, 31 and 35px — unchanged from v1, since the edits did not change
any line count. Longest sustained dull-orange run 0.20s against a 0.45s
threshold. v1 archived as `reel28-receiving-fouls.v1.mp4`; the new cut keeps the
unsuffixed filename, as the title is unchanged.

This rebuild does not improve on v1 by any *measurement* — v1 was clean on
every metric. The defect was the words, and the words are what changed.
