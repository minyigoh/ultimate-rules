# reel-32 — script feedback rounds

Rounds on the **script** gate. Rendered-content rounds live in `feedback.md`.

---

## Round 1 — 2026-09-04 (Content Desk, script track)

**Status on the desk:** `pending` with a note attached — not `changes`. Treated
as a script round anyway, because the note asks a substantive question about the
copy and the post has not been rendered, so acting on it now costs nothing and
leaves nothing for her to re-ask.

> Isn't contact part of a foul call? Infraction is infraction right?

**Redrafted:** 2026-09-04 (daily-reel-render)

Both halves of the note are correct, and v1's framing is what invited them.

- **"Isn't contact part of a foul call?"** Yes — contact is the *definition* of
  a foul. 15.1: "A breach of the rules due to non-minor contact between two or more opposing players is a foul." So the marker's bump is a foul from the moment it
  happens.
- **"Infraction is infraction right?"** Also yes. 15.2: "A breach of the rules regarding a Marking or Travel breach is an infraction. Infractions do not stop play." An
  infraction is defined by the kind of breach it is, not by who calls it.

v1 said "the same contact gives you two calls", which reads as though "Foul" and
"Contact" name two different events. They don't. 17.6.1.3 does not reclassify
the breach; it offers the thrower a choice of **remedy** — "the thrower may
choose to call a contact infraction" — for a Defensive Throwing Foul that its
own opening clause says has already occurred. The reel now says that out loud
instead of sliding past it.

What changed:

- **A third topic/rules pair, carding 15.1 and 15.2.** Scene 4 is the new
  `#2   STILL A FOUL, DIFFERENT CALL`, headline `"Contact" is not a different
  event.`, answering the note directly with the two definitions reel 21 already
  taught. Seven scenes to nine, the standard three-pair shape.
- **The hook.** "You can stop the game over it — or you can make the call that
  keeps you playing" → "That is a foul — and you still don't have to stop the
  game over it." The concession comes first now, so the reel never implies the
  contact was anything other than a foul.
- **The old `#2` becomes `#3`, and its kicker changes.** `THE CALL THAT KEEPS
  PLAY LIVE` → `YOU CHOOSE IF PLAY STOPS`. Incidentally retires the tightest
  kicker in the run: the old one measured 896 of the 900px column, four pixels
  of headroom. The new one is 772.
- **Both captions** gained the "if contact is what makes it a foul, how can it
  be an infraction?" section, quoting 15.1 and 15.2 in full, plus a closing line
  on the one limit — the choice only exists for contact before the release and
  not during the throwing motion. Block-quoted rule text elsewhere is untouched.
- **The example beat** now names the remedy explicitly: "What it changes is the
  remedy", then the two calls.

What did **not** change: the lesson number, the topic, the four rules v1 already
cited, the field tip, and the hashtag and attribution blocks. 18.1.3's
"minus one (1)" restart stays off every card — a contact infraction has its own
remedy under 17.6.1.3 and resumes at one, which is a further reason not to
describe it as simply "a marking infraction".

**Not asked for, and not done:** nothing in the note asks for a rule that
`rules.json` doesn't hold, and nothing in the redraft cites one. 15.1 and 15.2
are both in the source and both were carded in reel 21.

**On the desk now:** read the redraft. If it answers the note, click **Approve**
so the stamp is against the words you actually read. If it still doesn't, use
**Request changes** with a note — but don't click Request changes just to park
it for later, because the next daily run rewrites anything sitting at `changes`
and would replace this version before you'd read it.

---

## Round 2 — 2026-09-05 — CHANGES REQUESTED (script v2)
Can you double check the rules? Contact doesn’t fall under infraction right? Infractions are straddle, wrapping.

*(Heading and note above written by the desk via the Worker. Everything below is
the daily task's response to it — see the note at the end of this round on why
the two collided.)*

**Status on the desk:** `changes` on the script track and `rerender` on the
content track, both stamped 2026-09-05T03:00:02Z with the same note, tagged
**On-screen text**. The script had been approved against v2 seven minutes
earlier (02:53:44Z) and the v1 cut sent to content review at 02:59:53Z; the note
landed a moment later on both tracks.

**Redrafted:** 2026-09-05 (daily-reel-render) — script **v3**

**Checked, and she is right.** Both halves of the note hold against
`rules.json`:

- **The marking infractions are a closed, enumerated list.** 18.1.1 introduces
  it; 18.1.1.1–18.1.1.6 name every member — fast count, straddle, disc space,
  wrapping, double team, vision. "Contact" is not among them.
- **The categories are defined by the kind of breach, not by the call.** 15.1
  makes non-minor contact between opposing players a foul; 15.2 makes a Marking
  or Travel breach an infraction. A marker's bump is contact, so it is a foul,
  and it is neither a Section 18.1 marking breach nor a travel breach, so it is
  not an infraction.

**What v2 got wrong.** v2 answered round 1 correctly — the contact *is* a foul —
and then undid it on the same slide. Scene 4's body said the rule gives the
thrower "the choice to handle that foul the way an infraction is handled", and
scene 6 said "Contact" takes "the infraction route". With 15.2's general
definition carded directly underneath, the reel read as though Contact joined
the infraction family. It doesn't.

**The one thing that could not be written around.** 17.6.1.3 does use the
phrase — "the thrower may choose to call a contact infraction" — and that
sentence is quoted on scene 7. So v3 places the phrase rather than denying it:
it names a **call defined inside chapter 17, Fouls**, not a seventh entry on the
Section 18.1 list. Two things separate it from the real infractions — it is not
enumerated in 18.1.1, and it has its own remedy (the count restarts at one,
where the listed marking infractions restart at the last number uttered minus
one, 18.1.3). The one property it shares is the only one the reel claims:
uncontested, play does not stop.

What changed:

- **Scene 4's kicker** `#2   STILL A FOUL, DIFFERENT CALL` → `#2   A FOUL, NOT
  AN INFRACTION` (854 → 780 of the 900px column).
- **Scene 4's headline** `"Contact" is not a different event.` → `Contact is a
  foul, not an infraction.` The old one answered round 1 and said nothing about
  the category question. It also retires the reel's last headline `_payload()`
  case.
- **Scene 4's body** rewritten: it now names the six marking infractions and
  says plainly that Contact is not one of them.
- **Scene 5 gains a third block, 18.1.1.2 (Straddle)** — one of her two named
  examples, carded as concrete proof of what the category holds. Wrapping
  (18.1.1.4) was measured as a fourth block and overflows the card at 1562 of
  1310px, so straddle carries it and the body names the rest. Card max_y 740 →
  1026.
- **Scene 6's body** now quotes the "contact infraction" phrase and says where
  it lives, instead of calling it "the infraction route".
- **Both captions** rewritten across the same three points.
- **Scene 4's body returns to the standard 36px** (six lines, last baseline
  1062 of a 1090 limit). v2's version was the tightest body in the reel at 33px
  over seven lines.

What did **not** change: the lesson number, the topic, the hook, scenes 1–3,
the field tip, the closing, and the hashtag and attribution blocks. Every rule
v2 cited is still cited; 18.1.1.2 is the only addition.

**On naming the six.** Reels normally leave the 18.1 enumeration to lessons
17–19, but here it is the answer to the note, and all of those lessons have
already posted (disc space 08-22, straddle and wrapping 08-23, fast count
08-24, double team 08-25). It is a callback, not new curriculum. Only 18.1.1.2
is carded; the other five are named in body prose, which is written copy rather
than quoted rule text.

**Not asked for, and not done:** nothing in the note asks for a rule
`rules.json` doesn't hold. 18.1.3's "minus one" restart is the sharpest evidence
for the distinction and is still kept off every card and out of the body copy,
since 18.1's remedy machinery belongs to lessons 17 and 19.

**Not rendered this run.** The cut in this folder is v1, built from script v2,
and its scenes 4 and 5 no longer match the file. A redraft has to clear the
script gate before it is rebuilt, so the render waits for approval of v3.
`render_v3.py` has been updated to match, so the rebuild is mechanical.

**On the desk now:** read the redraft. If it answers the note, click
**Approve** — that re-stamps it against the words you actually read, and the
rebuild goes out on the next run. If it still doesn't, use **Request changes**
with a note. Please don't click Request changes just to park it for later
reading: the next daily run rewrites anything sitting at `changes` and would
write a fourth version before you'd read the third.

**Note on this round's merge conflict.** The desk now appends its own stub round
to `script-feedback.md` when a change is requested — a heading and the note
verbatim. That is new; rounds up to and including Round 1 were written only by
the daily task. On 2026-09-05 both sides appended a "Round 2" heading at the
same point in the file and the rebase conflicted on it. Resolved by keeping
**both**: the desk's heading and its verbatim note open the round, and the
task's analysis follows underneath, so nothing either side wrote was discarded
and there is only one Round 2 section.

The standing resolution rule — "anything that already existed takes the
remote/desk version" — does not decide this case, because neither side was
editing something that already existed; both were appending. For an append-only
log the rule to apply is **keep both, desk first**, and that is what future runs
should do here. The task should also expect this file to conflict on every
change-request round from now on, and should stop opening its own round with a
duplicate heading and a re-quote of the note the desk has already written.
